# Compress/encode a string. If the encoded string is longer than the input, 
# return the original input string, otherwise return the compressed string. 

#Encode base 64 

# Find each char in the accii table
# Get the binany number from that table 
# Join all the binany numbers and divide by 6 
# Get the number that correspond to that binany number of 6
# Find that number between the base 64  table.


def encode(string)
  string.each_char do |char|
    
  end
end